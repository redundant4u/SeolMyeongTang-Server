apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: seolmyeongtang-issuer-dev
spec:
  acme:
    email: rafch@naver.com
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: seolmyeongtang-issuer-dev
    solvers:
    - http01:
        ingress:
          class: traefik
